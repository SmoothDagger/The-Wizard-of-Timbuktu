using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActivateLevelFinish : MonoBehaviour {

    void OnTriggerEnter(Collider objecCollider)
    {
        if (objecCollider.gameObject.tag == "Player")
        {   
            
        }
    }
}
